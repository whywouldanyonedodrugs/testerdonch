# Donch Upgrade Jobtickets (2026-02-15)

Baseline evaluation confidence (current): `94/100`

Projected after all `P0/P1` tickets (including JT-012..JT-017): `96-98/100` (non-additive estimate).

## Priority Board

| ticket | status | priority | estimate | eta_calendar | focus | confidence_delta_points |
| --- | --- | --- | --- | --- | --- | --- |
| JT-001 | done | P1 | S | 0.5-1 day | Daily schema diagnostics in autopar | +4 |
| JT-002 | done | P0 | M | 1-3 days | Warmup-aware schema-fail classification from listing age | +6 |
| JT-003 | blocked | P0 | L | 3-5 days | Full live sizing parity with backtester mechanics | +9 |
| JT-004 | done | P1 | M | 1-3 days | Decision reason taxonomy normalization | +3 |
| JT-005 | done | P0 | L | 3-5 days | Intrabar replay parity harness | +7 |
| JT-006 | done | P1 | M | 1-3 days | Data freshness/completeness SLA checks | +5 |
| JT-007 | done | P1 | L | 3-5 days | Rolling walk-forward OOS validation pack | +10 |
| JT-008 | done | P0 | M | 1-3 days | Anti-leakage guards (regime + time alignment) | +8 |
| JT-009 | todo | P2 | M | 1-3 days | Execution realism stress matrix | +5 |
| JT-010 | todo | P2 | M | 1-3 days | Weekly parity drift dashboard | +3 |
| JT-011 | done | P0 | M | 1-3 days | Overfitting/stability proof gates | +9 |
| JT-012 | done | P1 | S | 0.5-1 day | Regime-sliced attribution and failure-bucket audit | +4 |
| JT-013 | done | P0 | M | 2-3 days | Risk-off containment sweep (block/downsize/probe) | +8 |
| JT-014 | done | P1 | M | 2-4 days | Regime-conditioned exits (TP/SL/time-exit variants) | +6 |
| JT-014b | in_progress | P1 | M | 2-4 days | Exit mechanics extension (partial/BE/trail/flip-time variants) | +6 |
| JT-015 | in_progress | P1 | M | 2-3 days | Regime-conditioned gate/sizing policy grid | +7 |
| JT-016 | in_progress | P1 | L | 3-5 days | Exploratory entry sweep + JT-011 stability gates | +7 |
| JT-017 | todo | P0 | S | 0.5-1 day | Locked train-confirm protocol for regime changes | +5 |

## Ticket Details

### JT-001: Daily schema diagnostics in autopar
- status: `done`
- priority: `P1`
- estimate: `S`
- owner: `offline`
- deliverables:
1. `live_schema_diag.json` per run.
2. Daily Telegram includes `schema_fail_rate`, top missing fields, top symbols.
3. No default/imputed feature values introduced.
- acceptance:
1. Diagnostics generated on normal and `no_signals` days.
2. `summary.json` includes `live_schema_diag`.

### JT-002: Warmup-aware schema-fail classification from listing age
- status: `done`
- priority: `P0`
- estimate: `M`
- owner: `online + offline`
- deliverables:
1. Add `symbol_listed_at` or `symbol_age_days` in export context.
2. Classify schema failures as `warmup_expected` vs `integration_defect`.
- acceptance:
1. Daily report explicitly separates expected warmup fails from defects.
2. Repeated non-warmup failures auto-flagged.
- evaluation impact: `+6` confidence, lower false alarms.
- completion evidence (team report, commit `e9c9ca7`):
1. Exchange metadata listing-age fetched and attached to decision exports:
   `symbol_listed_at_utc`, `symbol_age_days`.
2. `schema_fail_class` implemented (`warmup_expected`, `integration_defect`, `unknown`).
3. Export package now includes both `live_decisions.csv` (with class + age fields) and `symbol_metadata.csv`.
4. `schema_diagnostics.json` includes per-class counts, with warmup/non-warmup split.

### JT-003: Full live sizing parity with backtester mechanics
- status: `blocked`
- priority: `P0`
- estimate: `L`
- owner: `online`
- deliverables:
1. Match backtester sizing chain end-to-end:
   regime-down multiplier, risk_off probe cap, optional signal risk scale, notional/leverage cap.
2. Golden parity fixtures for size path.
- acceptance:
1. `size_mult` and `risk_usd` parity pass on fixtures.
2. No hidden fallback sizing path remains.
- evaluation impact: `+9` confidence, major live-vs-bt drift reduction.
- progress (team report, commit `e9c9ca7`):
1. Sizing chain implementation completed in live code (regime-down base sizing, risk_scale precedence, dynamic multiplier, risk_on/probe cap, risk-mode conversion, qty + notional/leverage cap, no secondary prob veto).
2. 100-row parity fixture passed:
   `size_mult_mean_abs_error=0.0`,
   `size_mult_p90_abs_error=0.0`,
   `risk_usd_mean_abs_error=0.0`,
   `risk_usd_p90_abs_error=0.0`.
3. Runtime log hook added for evidence:
   `SIZING_DECISION ... p_cal ... risk_on ... size_mult ... risk_usd ... qty`.
- blocker:
1. Acceptance evidence item requiring 5 real runtime sizing log lines is not yet met because current canary window has no actual entries.
- unblock steps:
1. Continue canary until at least 5 real entries occur (not synthetic replay).
2. Extract 5 `SIZING_DECISION` lines from `journalctl -u donch.service` including `p_cal`, `risk_on`, `size_mult`, `risk_usd`, `qty`.
3. Attach those 5 lines + timestamps to ticket evidence and flip status to `done`.

### JT-004: Decision reason taxonomy normalization
- status: `done`
- priority: `P1`
- estimate: `M`
- owner: `online + offline`
- deliverables:
1. Canonical reason codes map shared between live and backtest.
2. Compare reports normalize aliases before agreement metrics.
- acceptance:
1. Reason agreement metric stable across code versions.
- evaluation impact: `+3`.
- completion evidence (team report, commit `e9c9ca7`):
1. Canonical reason mapper implemented with both `reason_raw` and `reason_canonical` exported.
2. Mapping evidence provided (`reason_mapping.csv`, `reason_sample_20.csv`).
3. Canonical agreement artifact produced:
   `reason_agreement.json` with `canonical_agreement_rate=1.0` (also echoed in `run_context.json`).

### JT-005: Intrabar replay parity harness
- status: `done`
- priority: `P0`
- estimate: `L`
- owner: `offline`
- deliverables:
1. Replay harness comparing order of touches/exit reasons with 1m tie-break logic.
2. Edge-case fixtures for ambiguous bars.
- acceptance:
1. Deterministic parity across replay fixtures.
- evaluation impact: `+7`.
- artifacts:
1. `tools/intrabar_replay_parity_harness.py`
2. `unit_tests/fixtures/intrabar_replay_fixtures.json`
3. `docs/JOBTICKETS/JT-005_INTRABAR_REPLAY_HARNESS.md`
- verification:
1. `./.venv/bin/python tools/intrabar_replay_parity_harness.py --run-id jt005_baseline`
2. Result: `status=ok`, `checks=18`, `fail=0`, `det_fail=0` (`results/intrabar_replay_parity/jt005_baseline/summary.json`).

### JT-006: Data freshness/completeness SLA checks
- status: `done`
- priority: `P1`
- estimate: `M`
- owner: `offline`
- deliverables:
1. Daily checks for stale/missing OHLCV, OI, funding inputs.
2. Alerts when coverage drops below threshold.
- acceptance:
1. Autopar report includes input freshness summary.
- evaluation impact: `+5`.
- artifacts:
1. SLA checker integrated in `tools/donch_autopar_daily.py`.
2. Per-run outputs:
   `data_sla_diag.json`, `data_sla_details.csv`.
3. Report and Telegram include SLA summary/alerts.
- verification:
1. `./.venv/bin/python tools/donch_autopar_daily.py --run-id autopar_2026-02-14 ... --resume --sla-max-symbols 180`
2. `results/donch_autopar/autopar_2026-02-14/compare/summary.json` contains `data_sla_diag`.
3. `results/donch_autopar/autopar_2026-02-14/compare/report.html` contains `Data Freshness & Completeness (SLA)` section.

### JT-007: Rolling walk-forward OOS validation pack
- status: `done`
- priority: `P1`
- estimate: `L`
- owner: `offline`
- deliverables:
1. Automated rolling train/validate windows.
2. Calibration and rank-lift tracking by window.
- acceptance:
1. Published monthly OOS stability report.
- evaluation impact: `+10` (overfitting detection).
- artifacts:
1. `tools/run_walkforward_oos_validation.py`
2. `docs/JOBTICKETS/JT-007_ROLLING_WALKFORWARD_OOS.md`
3. Run outputs under `results/walkforward_oos/<run_id>/aggregate/`:
   `window_metrics.csv`, `monthly_metrics_by_window.csv`, `monthly_oos_stability.csv`,
   `monthly_oos_stability_report.md`, `monthly_oos_stability_report.html`.
- verification:
1. `./.venv/bin/python tools/run_walkforward_oos_validation.py --run-id jt007_smoke_20260215_b ... --max-windows 1 --n-splits 3`
2. Result: `windows=1`, `ok=1`, and report files present in
   `results/walkforward_oos/jt007_smoke_20260215_b/aggregate/`.

### JT-008: Anti-leakage guards (regime + time alignment)
- status: `done`
- priority: `P0`
- estimate: `M`
- owner: `offline`
- deliverables:
1. Tests enforcing filtered (past-only) regime probabilities in production path.
2. Time-alignment checks for all asof merges.
- acceptance:
1. CI-style script fails on leakage regressions.
- evaluation impact: `+8`.
- artifacts:
1. `tools/ci_check_leakage_guards.py`
2. `unit_tests/test_jt008_leakage_guards.py`
3. Explicit `merge_asof` contract hardening:
   - `backfill_trade_features.py`
   - `pull.py`
- verification:
1. `./.venv/bin/python tools/ci_check_leakage_guards.py --out results/ci_leakage_guards/report.json`
2. `./.venv/bin/python -m unittest -q unit_tests.test_jt008_leakage_guards`
3. Guard report: `results/ci_leakage_guards/report.json` -> `status=ok`, `violation_count=0`.

### JT-009: Execution realism stress matrix
- status: `todo`
- priority: `P2`
- estimate: `M`
- owner: `offline`
- deliverables:
1. Batch backtests over slippage/spread stress levels.
2. Sensitivity report for PnL and drawdown.
- acceptance:
1. Strategy remains viable under conservative assumptions.
- evaluation impact: `+5`.

### JT-010: Weekly parity drift dashboard
- status: `todo`
- priority: `P2`
- estimate: `M`
- owner: `offline`
- deliverables:
1. Trend plots for overlap, enter agreement, schema_fail_rate, and size drift.
2. Weekly Telegram digest.
- acceptance:
1. Drift can be monitored without manual digging.
- evaluation impact: `+3`.

### JT-011: Overfitting/stability proof gates
- status: `done`
- priority: `P0`
- estimate: `M`
- owner: `offline`
- deliverables:
1. Define explicit pass/fail gates on rolling OOS output (minimum windows, calibration gap ceiling, lift stability floor, worst-window bound).
2. Add an automated evaluator that reads `results/walkforward_oos/*/aggregate/*` and outputs `stability_verdict.json`.
3. Publish a one-page executive summary: pass/fail, key risks, and recommended live-risk mode.
- acceptance:
1. Full-horizon walk-forward run is scored by fixed gates (not discretionary).
2. Report includes both central tendency and tail-risk months/windows.
- evaluation impact: `+9` (explicit overfitting control).
- artifacts:
1. `tools/evaluate_walkforward_stability.py`
2. `docs/JOBTICKETS/JT-011_OVERFITTING_STABILITY_GATES.md`
3. Full-horizon verdict outputs:
   - `results/walkforward_oos/jt007_full_20260215/aggregate/stability_verdict.json`
   - `results/walkforward_oos/jt007_full_20260215/aggregate/stability_executive_summary.md`
   - `results/walkforward_oos/jt007_full_20260215/aggregate/stability_executive_summary.html`
- verification:
1. `./.venv/bin/python tools/evaluate_walkforward_stability.py --root results/walkforward_oos --run-id jt007_full_20260215`
2. Result recorded:
   - `verdict=fail`
   - failed gates: `positive_month_ratio`, `worst_window_mean_pnl_R`
   - all hard gates passed; two soft gates failed.

### JT-012: Regime-sliced attribution and failure-bucket audit
- status: `done`
- priority: `P1`
- estimate: `S`
- eta_calendar: `0.5-1 day`
- owner: `offline`
- deliverables:
1. Produce per-regime attribution tables (PnL_R, drawdown, hit-rate, n-trades) for:
   `risk_on` vs `risk_off`, ETH histogram sign, BTC vol-high regime.
2. Add explicit failure buckets for no-trade windows (`no_signals`, `schema_fail`, `gate_fail`, `scope_fail`, `below_pstar`).
3. Publish a one-page monthly regime health report.
- acceptance:
1. Report clearly separates "no-edge regime" from "pipeline failure" causes.
2. At least 95% of skipped decisions assigned to a canonical bucket.
- evaluation impact: `+4`.
- artifacts:
1. `tools/donch_autopar_compare.py` now emits:
   `failure_bucket_audit.csv`, `failure_bucket_rows.csv`,
   `regime_attribution.csv`, `monthly_regime_health.csv`,
   `monthly_regime_health_report.md`.
2. `tools/donch_autopar_daily.py` no-signals summary/report now include `failure_bucket_audit`.
3. `unit_tests/test_jt012_autopar_compare.py` covers bucket assignment and regime attribution smoke logic.
- verification:
1. `./.venv/bin/python tools/donch_autopar_compare.py --live-decisions /tmp/donch_autopar_smoke/live_decisions.csv --bt-decisions results/donch_autopar/smoke_orch/reference/backtest/signal_decisions.csv --bt-trades results/trades.csv --outdir results/donch_autopar/smoke_jt012_compare_with_trades --min-overlap-rate 0.0 --min-enter-agreement 0.0`
2. Result (`results/donch_autopar/smoke_jt012_compare_with_trades/summary.json`):
   `failure_bucket_assigned_rate_live=1.0` (>= 0.95),
   regime attribution rows present for `risk_on`, `eth_hist_sign`, `btc_vol_regime`,
   monthly regime health report generated.
3. `./.venv/bin/python -m unittest -q unit_tests.test_jt012_autopar_compare` -> `OK`.

### JT-013: Risk-off containment sweep (block/downsize/probe)
- status: `done`
- priority: `P0`
- estimate: `M`
- eta_calendar: `2-3 days`
- owner: `offline`
- deliverables:
1. Run constrained grid over `REGIME_BLOCK_WHEN_DOWN`, `REGIME_SIZE_WHEN_DOWN`, `RISK_OFF_PROBE_MULT`.
2. Add adverse-regime KPIs:
   worst-month mean PnL_R, worst-window mean PnL_R, downside deviation, drawdown tail.
3. Recommend one "default containment profile" and one "conservative fallback profile".
- acceptance:
1. Candidate chosen by fixed guardrails (not best raw PnL).
2. Selected profile improves at least one failed JT-011 soft gate without breaking hard gates.
- evaluation impact: `+8`.
- progress:
1. Implemented runner: `tools/run_jt013_risk_off_containment.py`.
2. Added adverse-regime KPIs:
   `worst_month_mean_pnl_R`, `worst_window_mean_pnl_R`,
   `downside_deviation_R`, `drawdown_tail_p05_R`.
3. Added fixed guardrail evaluator + automatic profile selection:
   `hard_proxy_pass`, soft-gate improvement checks, `recommendation.json`.
- smoke evidence:
1. Run: `results/jt013_risk_off_sweeps/jt013_smoke_20260215_b`.
2. Artifacts generated:
   `summary.csv`, `evaluation.csv`, `recommendation.json`, `report.md`.
3. Window KPI sanity fixed for small samples (no `NaN` from oversized rolling window).
- full-run evidence:
1. Run id: `jt013_full_20260215` completed.
2. Path: `results/jt013_risk_off_sweeps/jt013_full_20260215`.
3. Final artifacts present:
   `summary.csv`, `evaluation.csv`, `recommendation.json`, `report.md`, `advanced_metrics.csv`.
4. Acceptance check in `recommendation.json`:
   `improves_failed_soft_gate_any=true`, `passes_hard_proxy=true`.
5. Selected profiles exported:
   default `block_T__size_100__probe_005`,
   conservative `block_T__size_100__probe_001`.

### JT-014: Regime-conditioned exits (TP/SL/time-exit variants)
- status: `done`
- priority: `P1`
- estimate: `M`
- eta_calendar: `2-4 days`
- owner: `offline`
- deliverables:
1. Add policy presets where risk-off uses tighter/faster exits than risk-on.
2. Compare baseline exits vs regime-conditioned exits on walk-forward windows.
3. Publish sensitivity matrix for `SL_ATR_MULT`, `TP_ATR_MULT`, `TIME_EXIT_HOURS` by regime.
- acceptance:
1. Exit policy change must improve downside metrics in risk-off windows.
2. No degradation of hard stability gates from JT-011.
- evaluation impact: `+6`.
- progress:
1. Backtester supports explicit risk-on vs risk-off exit overrides:
   `REGIME_COND_EXITS_ENABLED`,
   `RISK_ON_*`,
   `RISK_OFF_*`.
2. Implemented runner: `tools/run_jt014_regime_conditioned_exits.py`.
3. Implemented tmux launcher: `tools/run_jt014_tmux.sh`.
4. Added ticket doc: `docs/JOBTICKETS/JT-014_REGIME_CONDITIONED_EXITS.md`.
5. Smoke evidence (fallback safety):
   `results/jt014_regime_exits/jt014_smoke_20260215_c`
   (`selection_mode=fallback_baseline_no_strict_candidate`).
6. Smoke evidence (strict candidate found):
   `results/jt014_regime_exits/jt014_smoke_20260215_d`
   (`recommended_setting=ro_sl200_tp600_te24`, strict candidate pass).
7. Fixed recommendation safety:
   if no strict candidate passes guardrails, fallback recommendation is baseline
   (`selection_mode=fallback_baseline_no_strict_candidate`).
8. Full run:
   `results/jt014_regime_exits/jt014_full_20260215`
   (selection: `fallback_baseline_no_strict_candidate`, recommended `baseline_exits`).

### JT-014b: Exit mechanics extension (partial/BE/trail/flip-time variants)
- status: `in_progress`
- priority: `P1`
- estimate: `M`
- eta_calendar: `2-4 days`
- owner: `offline`
- deliverables:
1. Expand exits in controlled mode on existing signals:
   `PARTIAL_TP_ENABLED`, `MOVE_SL_TO_BE_ON_TP1`, `TRAIL_AFTER_TP1`, `TRAIL_ATR_MULT`.
2. Add regime-flip/time-stop mechanics variants:
   `EXIT_ON_REGIME_FLIP`, `EXIT_ON_REGIME_FLIP_GRACE_BARS`, shorter risk-off time exits.
3. Keep strict guardrails unchanged (cash/sharpe/calmar floors + risk-off downside/tail improvements).
- acceptance:
1. Any selected variant must pass existing guardrails.
2. If no strict candidate exists, runner must fallback to baseline (no forced change).
- evaluation impact: `+6`.
- progress:
1. Backtester support added for `EXIT_ON_REGIME_FLIP` and grace bars.
2. `tools/run_jt014_regime_conditioned_exits.py` now supports `--preset-mode mechanics`.
3. Variant-level progress telemetry added:
   `_PROGRESS.json` + Telegram `PROGRESS` updates.
4. New launcher: `tools/run_jt014b_tmux.sh`.
5. Smoke run passed:
   `results/jt014_regime_exits/jt014b_smoke_20260216_b`.

### JT-015: Regime-conditioned gate/sizing policy grid
- status: `in_progress`
- priority: `P1`
- estimate: `M`
- eta_calendar: `2-3 days`
- owner: `offline + online`
- deliverables:
1. Evaluate policy grid for risk-off:
   higher `pstar`, tighter `scope`, lower size multiplier bounds.
2. Add parity-ready config profile for live:
   explicit values for threshold, scope, `META_SIZING_MIN/MAX`, probe cap.
3. Generate decision-volume vs quality tradeoff chart (coverage, win-rate, PnL_R).
- acceptance:
1. Chosen profile has documented tradeoff and reproducible config.
2. Live parity manual updated with final selected profile and fallback profile.
- evaluation impact: `+7`.
- progress:
1. `tools/sweep_policy_settings_v2.py` now has Telegram notifications:
   `STARTED`, `PROGRESS`, `DONE/WARN`.
2. Progress cadence controllable via `--notify-every-variants`.
3. New launcher: `tools/run_jt015_tmux.sh`.

### JT-016: Exploratory entry sweep + JT-011 stability gates
- status: `in_progress`
- priority: `P1`
- estimate: `L`
- eta_calendar: `3-5 days`
- owner: `offline`
- deliverables:
1. Exploratory entry sweep over constrained set:
   `DON_N_DAYS`, retest strictness (`RETEST_EPS_PCT`, `RETEST_LOOKBACK_BARS`),
   pullback window, volume spike threshold (`VOL_MULTIPLE`), RS percentile.
2. Generate fresh signals per variant (scout) and backtest each variant on same date span.
3. Run JT-011 stability evaluator on top candidates (walk-forward + fixed gates), not raw PnL alone.
4. Produce recommendation chosen by:
   JT-011 pass first, then total PnL cash as tie-break.
- acceptance:
1. Final recommendation must include JT-011 verdict and failed gates (if any).
2. Result includes both trade metrics and walk-forward stability evidence.
- evaluation impact: `+7`.
- progress:
1. Implemented runner: `tools/run_jt016_entry_exploratory.py`.
2. New launcher: `tools/run_jt016_tmux.sh`.
3. Smoke run passed:
   `results/jt016_entry_sweeps/jt016_smoke_20260216_b`.

### Execution Order (Operational)
1. Run `JT-014b` first (exit mechanics on existing signals).
2. Run `JT-015` second (gate/sizing policy).
3. Run `JT-016` third (entry exploration + JT-011 gated selection).
4. Use orchestrated runner for stage notifications and stop-on-failure:
   `tools/run_jt_pipeline_14b_15_16.py` or `tools/run_jt_pipeline_14b_15_16_tmux.sh`.

### JT-017: Locked train-confirm protocol for regime upgrades
- status: `todo`
- priority: `P0`
- estimate: `S`
- eta_calendar: `0.5-1 day`
- owner: `offline`
- deliverables:
1. Define fixed protocol:
   tune only on development windows, confirm once on untouched holdout windows.
2. Add CI-style checker rejecting runs that use confirmation windows for tuning.
3. Publish protocol checklist in docs used before each release.
- acceptance:
1. Every regime-policy change must include dev/confirm split evidence.
2. Release note includes "no confirmation leakage" statement.
- evaluation impact: `+5`.
