# Received task — Stage 24 launch-packet interface remediation

Proceed with one narrowly bounded Stage 24 launch-packet interface remediation. Preserve continuity sequence 8 and its bound-stop record; the published commit and Drive handoff as immutable provenance; all caches, registries, reused evidence, markers, benchmarks, tests and independent-review artifacts; the empty economic run root; and confirmation that zero economic units executed.

Do not modify or rerun economic, selection, cache, family, KDA02B, control, benchmark, materialization or forensic work.

Repair the final packet builder so `FINAL_CAMPAIGN_MANIFEST.json` contains the complete top-level `launch_population_authority` and `kda02b_lazy_population_authority` mappings required by `CampaignOrchestrator._authority`. Derive their exact schema from the production parser, working Stage 24 shadow manifests/specifications, and the already verified authority artifacts. Mappings must point to the verified artifacts and equal their corresponding `primary_hashes`. Do not add a permissive runtime fallback.

Mandatory validation: builder presence, real parser schema, mapping/hash equality, physical path/size/hash, missing/malformed/mismatched fail-closed cases, regenerated packet round-trip/inventory, actual final launch CLI/service preflight, and one detached `shadow_no_outcome` canary using the exact regenerated final manifest. The canary must construct both authorities through the real runtime, reconcile one production-shaped registered unit, send Telegram preflight/launch/health/completion, open no economic/protected/Capital.com outcome, and leave no orphan process.

Reuse all prior evidence after a machine-readable delta audit. Do not rerun the 1,984 structural units, KDA02B adjudication, 1,200-unit benchmark, A2 development, outer evaluation, controls, materialization, forensics, production cache, or full shadow. Only old manifest-dependent packet identities and the failed launch receipt are invalidated.

After focused PASS: one limited independent review with zero material blockers; narrow commit; non-force update of `main` and `origin/main`; new round-trip Drive handoff; next continuity sequence referencing sequence 8; and return exact replacement approval hashes. Do not launch economic work without replacement human approval. Return only the replacement approval block and a concise narrow-delta explanation.
