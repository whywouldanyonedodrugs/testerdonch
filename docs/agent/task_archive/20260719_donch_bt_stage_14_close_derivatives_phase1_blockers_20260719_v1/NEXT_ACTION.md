# Next Action

After repository integration and verified Drive handoff, obtain an external human-approval artifact that explicitly names the final `FUTURE_DERIVATIVES_CAMPAIGN_APPROVAL_PACKET.json` SHA-256. Do not begin Phase 2 before that artifact exists and all bound hashes still match.
