# KDA03 Independent Pre-Outcome Review

Status: `blocked_preoutcome`
Approved: `false`
Frozen Level-3 contract hash: `d20ae93320bba39fb4b7231532a282e42263580093960b13c7ee681d76429ad0`

No economic run was launched. No official post-decision open price, candidate return, funding outcome, protected-period data, executed control payload, KDA01/KDA02 outcome, or Capital.com payload was opened. `tools/run_kda03_level3.py` was not run.

## Decision

The frozen tapes and mechanical schedule passed the independent outcome-free checks, but the economic gate remains closed because the prospective policy contract is incomplete and the runner does not fail closed on drift of the external official-open authority. Repair these two surfaces, produce a new frozen contract and matching artifacts, and obtain another independent pre-outcome review before any Level-3 execution.

## Findings

### KDA03-PRE-001 — blocking — prospective policy fields are not explicitly frozen

- Path and line: `docs/agent/task_archive/20260719_donch_bt_stage_11_kda03_basis_shock_20260719_v2/KDA03_FINAL_LEVEL3_CONTRACT.json`; policy authority `docs/agent/RESEARCH_GATE_ROUTING_POLICY.json` under `required_contract_fields`.
- Observed behavior: definitions freeze `payoff_archetype` and `intended_claim_scope`, but the contract has no explicit top-level `hard_gates`, `routing_diagnostics`, `control_eligibility`, or `independent_evidence_requirement` fields. Partial analogues (`mechanical_gates`, `routing_policy`, `controls`) do not state the full prospective integrity, diagnostic, authorization, and independent-evidence contract.
- Required contract: policy v1.0 requires all six named fields to be frozen before outcomes. Root repository instructions repeat this requirement for every future economic contract.
- Consequence: approving the current file would leave required policy semantics implicit and mutable at the exact boundary where the one authorized economic run becomes available.
- Smallest repair: add the four missing explicit fields using the already-approved policy vocabulary, without changing any KDA03 definition or threshold; regenerate the contract hash and dependent freeze artifacts; then rerun independent pre-outcome review.
- Verification: assert all `required_contract_fields` from policy JSON are represented explicitly by the frozen contract and that values match policy v1.0.

### KDA03-PRE-002 — blocking — official-open data authority drift is not enforced before outcome reads

- Path and line: `tools/run_kda03_level3.py:359`, `tools/run_kda03_level3.py:361`, `tools/run_kda03_level3.py:367`, and `tools/run_kda03_level3.py:423`; frozen timestamp hash written by `tools/build_kda03_v1_prerun_freeze.py:616`.
- Observed behavior: the runner reconstructs `timestamp_hash`, but never compares it with `contract["timestamp_authority_hash"]`. The market-manifest SHA-256 is recorded only after the run in `RUN_AUDIT.json`, not frozen and checked before reads. `load_open_map` also reads selected parquet objects without verifying their bytes against each manifest row's declared `parquet_sha256`.
- Required contract: data and execution authority must be frozen, content-hash verified, and fail closed before official opens enter outcome computation; deterministic replay must be tied to the reviewed authority.
- Consequence: a changed manifest or in-place bar payload could feed economic outcomes despite an otherwise valid review, and some timestamp drift could escape the primary-count-only reconciliation.
- Smallest repair: freeze the market-manifest SHA-256 and selected official trade-bar authority hash in the Level-3 contract; verify the manifest file and each selected parquet's full SHA-256 before reading timestamps or opens; compare reconstructed `timestamp_hash` with the frozen value before output creation and before `price_and_score`; add a focused failure-path test. Refreeze and rereview.
- Verification: mutate each authority input in a temporary synthetic fixture and prove the runner stops before the open reader; verify the unchanged authority reproduces the frozen schedule and hash.

### KDA03-PRE-003 — medium — feature-contract prose misstates basis normalization

- Path and line: `docs/agent/task_archive/20260719_donch_bt_stage_11_kda03_basis_shock_20260719_v2/KDA03_V1_FEATURE_EXTENSION_CONTRACT.md`; generated by `tools/build_kda03_v1_prerun_freeze.py:630`.
- Observed behavior: the prose says liquidation uses daily maxima and “other states use daily medians.” The source contract and implementation instead score `basis_change_15m` against all valid observations from prior UTC days; prior basis, price displacement, and OI use prior daily medians; liquidation intensity uses prior daily maxima.
- Required contract: narrative and machine/source semantics must agree exactly before an economic run.
- Consequence: the hash-bound source is unambiguous, but the human-facing feature contract describes a different basis normalization population.
- Smallest repair: correct that sentence only, regenerate the artifact manifest, and include it in the refreeze review.
- Verification: compare the corrected prose with `FEATURE_EXTENSION_CONTRACT` and `_normalize_prior_observations`.

## Outcome-Free Verification Results

- Focused synthetic tests: `20 passed, 0 failed` via `/opt/testerdonch/.venv/bin/python -m unittest unit_tests.test_kda03_v1 unit_tests.test_kda03_level3`.
- The repository-documented system-Python pytest command was unavailable: system Python had no `pytest`, `numpy`, or project dependencies. This environment issue did not affect the venv test result.
- Stage 8A adjudication: `186,265 + 247,169 + 60,836 = 494,270`; source matrix hash matched the frozen builder authority.
- Feature cache: `187` shards, `39,279,314` rows, and all `561` feature/episode/event parquet hashes matched their shard manifests.
- Frozen tapes: `104,937` unique parent episodes and `113,539` unique events/economic addresses; duplicate episode/event/economic-address counts `0/0/0`.
- Rankable boundary: pre-2023/protected event counts `0/0`; protected accepted exits `0`; protected outcomes opened `0`.
- Exact clock: all event decisions equal `state_ts + 5m`; accepted entries are the first authorized timestamp at or after decision with delay at most 10m; exits are at or after actual entry plus the frozen timeout with delay at most 10m.
- Parent predicates: all `104,937` frozen onsets independently satisfied the attempt, direction, availability, KDA03A/KDA03B-C, and exact-window gates.
- Episode/rejection rules: all episode ends were after availability and no later than six hours; candidate/rejection counts reconciled for every episode. All `8,602` rejection events satisfied basis+trade+mark completed-close confirmation and had no earlier qualifying completed bar.
- Direction rules: catch-up and rejection events trade opposite the signed shock; continuation events trade with it; mismatches `0`.
- Actual-exit non-overlap: `199,787` accepted definition-event executions; definition-local/symbol-local overlap violations `0`; duplicate definition-event schedule rows `0`.
- Mechanical gates: all six primary branches and both 1h/6h definitions met `>=100` accepted candidates, `>=50` market-day clusters, `>=10` symbols, zero duplicate identities, and zero protected exits. Final register contains `12` primary and `12` non-rescuing robustness definitions.
- Determinism: all `113,539` event IDs/economic addresses and all `104,937` episode IDs recomputed exactly; consolidated tape core rows reconciled bidirectionally with the frozen per-symbol shards (`0` rows missing or extra).
- Contract/hash checks: the Level-3 canonical hash recomputed exactly; all 24 definition hashes recomputed exactly; all six code/test hashes embedded in the contract matched; policy SHA-256 matched `c54d4a3445da249c6dbd34613b770ad9624f861e0d7c345b3cba944aa6cdf1aa`.
- Runner ordering: review approval and the exact nine-file hash set are checked before event parquet reads; schedule reconstruction precedes `price_and_score`. The missing external-authority comparisons in KDA03-PRE-002 prevent approval.

## Reviewed SHA-256 Set

The machine-readable companion contains exactly the nine keys required by the runner preflight. No source, contract, definition, tape, gate, or control artifact was modified by this review.

## Remaining Evidence Limits

The cohort remains current-roster and lifecycle capped, not survivorship-free. Only 55 of the 187 Stage 8A symbols emitted KDA03 candidates. OI history is truncated in early 2023, basis/OI/liquidation units remain `inferred_authoritative_v1`, and KDA03A remains a directional PF-futures proxy rather than a spread or arbitrage test. These are claim caps, not the reason approval is withheld.
