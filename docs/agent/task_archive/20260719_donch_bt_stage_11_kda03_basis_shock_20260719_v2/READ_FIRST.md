# Read First

Stage 11 terminal status: `KDA03_level3_routes_assigned`.

Start with `COMPLETION.md`, `VALIDATION.md`, and `REVIEW.md`. Exact primary and robustness metrics, routes, bootstrap summaries, concentration identities, context diagnostics, and funding partitions are under `run_outputs/`. Full event, episode, schedule, trade, bootstrap, and funding Parquet artifacts remain local and are hash-linked by the manifests.

The only non-rejected primary definition is negative completed-basis rejection at six hours, routed `sample_limited_prospective_candidate`. It is not control-eligible and is not validated. No control, protected-period, Capital.com, KDA01/KDA02 rescue, portfolio, live, or production action is authorized.
