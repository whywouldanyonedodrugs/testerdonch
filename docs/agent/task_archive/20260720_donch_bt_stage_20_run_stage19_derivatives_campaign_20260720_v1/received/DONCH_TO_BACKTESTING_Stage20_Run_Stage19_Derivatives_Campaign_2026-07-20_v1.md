# Stage 20 — Run the exact Stage 19 derivatives campaign through Phases 2–5

```text
task_id: donch_bt_stage_20_run_stage19_derivatives_campaign_20260720_v1
repository: /opt/testerdonch
mode: direct_apply_in_isolated_worktree
drive_handoff: approved_default
expected_starting_main_and_origin_main: 245b375b00167f1b4a81f6a4449e7de1d1db83a2
commit_authorized: yes, task-scoped only
fast_forward_merge_authorized: yes, only from the exact clean starting main
push_authorized: yes, non-force only
```

## Sole economic authority

Use the attached exact external approval artifact and no prior approval:

```text
file: HUMAN_APPROVAL_Kraken_Derivatives_Campaign_001_Stage19_Phases_2_5_2026-07-20_v1.json
SHA-256: 57d521488e88373afd557eb457ffd119089aac0d863a0c319e73d70cf9f7690c
```

It authorizes one protocol-v2 campaign execution only for:

```text
campaign manifest SHA-256:
  e7d618a2a24c574c9ba83d323df605a6434c2e816fe24c465d183ba5d6256990
approval packet SHA-256:
  3fde09f16efff2479ee847fe26c859a67ef1d516b0f0acbcbaec141154a87bd6
phases: 2, 3, 4, 5
lanes:
  KDA02B_v2_oi_vacuum_redevelopment — 96 cells
  KDA02C_v1_purge_breadth_context — 48 cells
  KDX01_v1_downside_completed_derivatives_state_rejection — 42 cells
executable cells: 186
inherited non-executable KDX attempts: 42
programme attempts reconciled: 228
```

All 2023–2025 outputs are `program_exposed_historical`. They are registered development, deterministic freeze, forward outer evaluation, and rolling replication evidence only. They are not independent validation, validation-grade evidence, deployment evidence, or live readiness.

## Authority and preflight

Before changing files or opening any outcome reader:

1. Verify `/opt/testerdonch`, `AGENTS.md`, remotes, worktrees, archive convention, Drive target, free RAM/disk/CPU, and a clean `main`/`origin/main` exactly at `245b375b00167f1b4a81f6a4449e7de1d1db83a2`. Stop on drift or unrelated work.
2. Fetch and byte/hash-verify continuity pointer sequence 1, pointer SHA-256 `28ef052ff9bb7c2523accec5c8acd36e4f903ac7efd84be44cd3972faaae1c00`, and snapshot `snapshots/state_000001_20260720T121247Z.json` physical SHA-256 `c3bdfa6fa7c9d9427e4b39ca109262f0becd0ff004d145a5a1674694b61094bf`.
3. Verify the immutable Stage 19 handoff at `https://drive.google.com/drive/folders/1Iqhr3hXx4zZYvQqr1IqA5yqXuopSFavB`; archive SHA-256 `a7065fa8b88cdd9bc47e03e71c86af83735d3b31178cdacc91fdabf3343c9e90`.
4. Preserve the Stage 17 protected-funding access incident. Treat Stage 19 protected funding partitioning as authorized non-outcome engineering, not an incident.
5. Copy the exact approval artifact into `received/`, verify its SHA-256, then validate the campaign manifest, approval packet, every dependency hash, 186-cell registry, 27-fold map, and 42 inherited non-executable attempts.
6. Validate the official rankable funding package and runtime adapter: package SHA-256 `6c0969727c882ca6439c57c3b7d03367e1b2d26ee46823e56b983756d026ef64`; 187/187 symbols mapped and unit-verified; both exact hourly alignments; adverse minimum of zero and both alignments; nonpositive rankable-only q95/q99 missing-hour charges; no favourable funding rescue; zero protected values used for statistics. Runtime may not choose an alignment.
7. Compile and run focused campaign/funding/protected-safe tests, the launch validator, and the synthetic Phase 2–5 canary. The canary must compute no real economic outcome.
8. Obtain an independent read-only pre-outcome review confirming the exact approval, hashes, funding/runtime behavior, outcome firewall, deterministic replay, resource limits, and launch readiness.
9. Use only the existing secure Telegram notifier. Before outcomes, verify a dry-run preflight, synthetic heartbeat, and synthetic stop alert without printing, archiving, committing, or returning secrets. If unavailable, stop as `blocked_telegram_notifier_unavailable`.

No real outcome reader may open until every gate passes.

## Frozen execution

Do not add, remove, merge, reinterpret, tune, or manually select any cell, feature, bin, threshold, side, instrument, horizon, fold, entry, exit, overlap rule, cost, funding rule, metric, Pareto rule, beam rule, or economic address. Mechanical wiring is allowed only when it implements the frozen packet without semantic choice; otherwise stop before outcomes and require a new packet.

Run Phases 2–5 exactly:

- Phase 2: execute every registered development cell or retain an exact mechanical skip/stop reason; preserve complete positive and negative response surfaces and all attempts.
- Phase 3: apply the frozen finite-value, Pareto, lexicographic, correlation, and maximum-five-candidate rules; freeze full economic addresses before each outer block; no manual candidate selection.
- Phase 4: evaluate frozen translations only, with registered native PF symbol/side, decision/entry/exit rules, definition-local actual-exit non-overlap, 14/32-bps scenarios, and the exact Stage 19 funding contract. Outer information cannot alter selection.
- Phase 5: continue through every authorized outer fold in order; preserve each frozen identity and every positive, negative, unavailable, or stopped fold.

Do not execute Phase 6 controls or any independent validation.

## Supervision, health release, and stops

Run under a persistent reviewed supervisor with atomic state/artifacts and idempotent recovery. Maximums: 4 workers, 14,400 seconds after economic launch, 5 GiB aggregate RSS, 5 GiB campaign output, and 5 candidates per family/fold. Send secure notifications for start, phase, fold, family stop, global warning/stop, completion, and 30-minute heartbeats. Heartbeats contain only operational state, never partial rankings or economic candidates.

After launch, observe only operational health until one real registered cell completes and reconciles, the first scheduled 30-minute heartbeat is delivered, and state/artifact hashes verify. Do not inspect its economic result. If healthy, leave the campaign unattended until terminal completion or a bound stop. Do not intervene based on outcomes.

A family-specific stop must not terminate unrelated lanes. Global stops include hash/authority drift, protected or Capital.com access, shared temporal/execution/cost/funding defect, deterministic replay or reconciliation failure, supervisor failure, resource/storage breach, or unsafe Git. On a bound stop, shut down gracefully and preserve exact resumable state; never change the packet to finish.

## Required closure

Reconcile exactly 186 executable cells, 42 inherited non-executable KDX attempts, and 228 programme attempts. Perform independent post-run recomputation/review of deterministic freezes, selected candidates, outer-fold results, rolling replication, multiplicity, routes, and limitation tags before making claims.

Archive the received approval, plan, decisions, commands/results, pre-outcome audit, funding/runtime validation, Telegram validation, campaign state/heartbeats, full attempt registries, response surfaces, frozen beam/translation registries, outer and rolling results, multiplicity/routes, family and campaign decisions, validation, independent review, artifact manifest, completion, and next action. Keep large economic tapes local and hash-manifested; send only the compact evidence package to Drive.

Use an isolated worktree. Preserve unrelated work. `git diff --check`, tests, secret scan, and final clean status must pass. Task-scoped commits, fast-forward integration, and non-force push are authorized only if the exact starting authority remains unchanged. Complete and round-trip verify `drive_handoff: approved_default` by size and SHA-256 and retain the local copy.

After the immutable handoff is verified, publish exactly one next-sequence dynamic continuity event and one complete snapshot, round-trip verify them, then transactionally advance the pointer. Do not publish credentials, private/account data, protected outcomes, raw payload values, or partial rankings. If pointer replacement fails after immutable publication, report `continuity_pointer_stale`; do not rerun economics.

## Explicitly unauthorized

Phase 6 controls; independent validation; C17; protected strategy outcomes; protected-period selection; new acquisition; Capital.com payload access/import; cohort or symbol expansion; capture restart; private/account actions; orders; risk changes; deployment; live trading; packet or semantic changes; hidden search; manual partial-result intervention; force push; destructive Git; canonical project-source or instruction replacement.

Return a compact terminal report with starting/final commits, approval/hash checks, pre-outcome review, funding/runtime and Telegram gates, supervisor/run root, planned/executed/skipped attempts, Phase 2–5 results and routes, multiplicity, controls=`no`, protected/Capital.com access=`no`, tests/reviews, artifact manifest, Git state, Drive handoff verification, continuity sequence/publication status, remaining limitations, and the smallest safe next action.
