# Next Task Recommendation

## Exactly one task

`Stage_1B_remaining_rankable_reader_boundary_closure`

## Objective

Apply the already implemented loader-local authority helper to only these three active sibling readers:

- `load_symbol_signal_bars()`;
- `load_symbol_rank_close_window()`;
- `a1_load_symbol_bars_window()`.

Add synthetic reader/downstream spies for each, and prove current file-manifest authority can be supplied without reading protected payloads. Do not build a catalog or partitioning framework.

## Allowed scope

- The three named functions and, only if needed, the existing `data_paths()` authority-map handoff.
- Focused synthetic tests and task archive.
- Existing historical manifest metadata inspection only.

## Stop conditions

- Any additional reader or generalized framework is required.
- Existing manifest metadata cannot prove purpose, venue, and complete pre-2026 scope.
- A real protected payload would need to be opened.
- Economic, strategy, universe, acquisition, capture, or package work would be required.

After this closes, rerun readiness again. Only then consider `Stage_2A_U2_and_common_feature_foundation`.
