# Donch Context

Stage 15 remains `blocked_preoutcome_packet_semantics`. Its validator repair is accepted. The Stage 14 manifest and approval packet remain immutable, non-authorizing provenance. Stage 16 creates a replacement version only; historical terminal decisions and programme-exposure lineage do not change.
