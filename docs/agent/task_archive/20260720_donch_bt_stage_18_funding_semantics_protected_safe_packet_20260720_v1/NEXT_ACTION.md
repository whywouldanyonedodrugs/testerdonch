# Next Action

The next task must supply two authorities before packet regeneration:

1. A physical rankable-only absolute `fundingRate` source created upstream without opening mixed/protected payload under the current policy, or new exact authorization for a one-time trusted transformation that explicitly governs protected handling.
2. An official Kraken statement binding the historical funding endpoint row timestamp to the accrual period it represents.

After those are available, rerun the protected-safe package builder, calibrate Type-7 symbol q95/q99 and equal-symbol fallback from exact rates joined to authorized period-start mark opens, freeze the uniform adverse-allowance selection contract, regenerate every funding-dependent economic address and packet, and obtain an independent pre-outcome review. A new exact human approval naming the replacement packet hashes is then required before any campaign economics.
