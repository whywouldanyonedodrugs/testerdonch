# C02 Leadership Generator Foundation Handoff

Status: `alignment_fragile_requires_review`.

This is the authoritative `v02` package. The earlier `v01` remote upload is superseded because final handoff-completion records changed the local artifact-manifest hash; no `v01` object was overwritten or deleted.

The package contains the outcome-free Stage 3B C02 spot/perpetual leadership generator contract, compact evidence, independent review, implementation, and focused tests. Mechanical generation passed, but the frozen +/-5-minute alignment stability gate failed. No C02 economic run is authorized.

Start with `C02_GENERATOR_REVIEW.md`, `VALIDATION.md`, and `C02_NEXT_CONTRACT_RECOMMENDATION.md` under the task archive path inside the ZIP. Large Parquet tapes, logs, raw market data, protected payloads, and capture data are excluded; local tape hashes remain in `ARTIFACT_MANIFEST.json`.
