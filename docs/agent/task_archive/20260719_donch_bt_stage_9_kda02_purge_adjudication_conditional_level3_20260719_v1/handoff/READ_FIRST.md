# Read First

Stage 9 terminal decision: `KDA02_level3_no_primary_pass_stop`.

Start with `COMPLETION.md`, `VALIDATION.md`, and `REVIEW.md`. Exact primary/robustness metrics and gates are under `run_outputs/`. The full local archive retains all Parquet tapes and bootstrap artifacts; compact handoff material is hash-linked through `ARTIFACT_MANIFEST.json`.

No controls or KDA02B outcomes were executed, and no protected row was opened. This evidence does not authorize KDA02 rescue, validation, portfolio construction, live trading, or production use.
