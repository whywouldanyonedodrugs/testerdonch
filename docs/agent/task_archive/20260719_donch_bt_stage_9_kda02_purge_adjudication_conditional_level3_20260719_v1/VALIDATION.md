# Validation

Outcome-free generation completed for `187` symbols, `8281` parent episodes, and `5128` candidates. Duplicate episodes/events/economic addresses and protected rows are `0/0/0/0`. Feasible primary branches: `2`. Economic outputs: `0`.

The superseding freeze reproduced byte-for-byte through an isolated cache-only reducer. The relevant repository suite passed `102` tests; the independent focused review passed `27` tests. Independent pre-outcome status is `approved` with no blockers and all nine runner-preflight hashes matched. PF trade-open values, returns, funding outcomes, controls, KDA02B outcomes, and protected rows remained unopened through approval.
