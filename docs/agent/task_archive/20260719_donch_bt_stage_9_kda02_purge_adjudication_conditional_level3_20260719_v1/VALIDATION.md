# Validation

Outcome-free generation completed for `187` symbols, `8281` parent episodes, and `5128` candidates. Duplicate episodes/events/economic addresses and protected rows are `0/0/0/0`. Feasible primary branches: `2`. Economic outputs: `0`.

The first reviewed runner invocation stopped before output-root creation and before any economic outcome read because schedule reconciliation included deliberately omitted infeasible gate rows. That failure is preserved. The surgical identity-intersection repair passed `103` relevant tests and a cache-only v5 rebuild; fresh independent approval of the changed runner and contract hashes is required before returning to the outcome boundary.
