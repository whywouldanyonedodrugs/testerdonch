# Validation

The finalized run passed all frozen authority, schedule, exact-open, arithmetic, equal-market-day, bootstrap, concentration, funding-separation, protected-boundary, deterministic-recompute, and artifact checks. `183,744` trades and `606,164` unique funding boundaries reconcile. Primary passes: `0/8`. Controls/protected rows: `0/0`.

Final relevant regression suite: `171/171` passed; focused KDA01 Level-3 suite: `10/10` passed.
