# Exact next action

Authorize or initiate one bounded Stage-22 implementation continuation that
closes all seven acceptance criteria in
`V04_INDEPENDENT_PREOUTCOME_REVIEW.json` as one scope, while preserving the
economic, protected-period and Capital.com firewalls. The replacement
candidate must receive another complete independent pre-outcome review.

Only a PASS may create the final campaign manifest, human approval request and
launch task or update `main`/`origin/main`.
