# Independent Review

Decision: approve after repairs and final archive closure.

The independent reviewer inspected the isolated staged change read-only and observed no strategy code, loader, simulator, test, data, finalized run-root, protected payload, Capital.com payload, or outside-documentation change.

## Binding questions

1. **Are integrity gates still hard?** Yes. Authority, hashes, protected exclusion, causality, deterministic identity, platform execution, reproducibility, independent review, and no same-sample rescue remain fail-closed.
2. **Does year concentration route rather than retroactively pass KDA02?** Yes. `KDA02_level3_no_primary_pass_stop` remains binding; the separate route is exactly `conditional_context_candidate_unvalidated`.
3. **Are payoff-archetype exceptions prospective only?** Yes. The archetype must be frozen before outcomes and cannot be relabelled afterward.
4. **Are post-hoc context hypotheses labelled and validation-capped?** Yes after repair. Both current registries carry exact `post_hoc_context_hypothesis`, and same-sample use is limited to explanation and separately authorized controls.
5. **Are KDA01/KDA02 terminal decisions and roots unchanged?** Yes. Finalized roots have no diff; the exact terminal tokens and manifest hashes match.
6. **Are controls still separately approval-gated?** Yes after repair. Eligibility is distinct from exact task authorization, and controls do not validate a candidate.
7. **Does the policy permit opportunity capture without same-sample rescue?** Yes. Conditional, convex, narrow, execution-sensitive, and sample-limited routes preserve bounded research objects while forbidding calendar-context selection, retroactive pass labels, validation, or deployment from the same sample.

## Findings and resolution

- **Blocking manual-field omission:** the active evidence manual omitted three exact contract fields and did not restate separate control-run authorization. Repaired with all six exact fields and an explicit eligibility/authorization distinction.
- **Blocking post-hoc vocabulary mismatch:** two registries used composite or shortened context labels. Repaired to exact `post_hoc_context_hypothesis` plus separate `conditional_context_candidate_unvalidated`.
- **Blocking route vocabulary mismatch:** the gate matrix used `conditional_context_candidate` and informal candidate tokens. Repaired so candidate-producing routes match the JSON status vocabulary and retain `_unvalidated`.
- **Blocking audit-scope workaround:** a temporary `.gitattributes` change suppressed diffs for exact CRLF sources. Removed; exact sources remain fully diffable, and whitespace validation uses Git's `cr-at-eol` interpretation.

## Final review evidence

- Active JSON and all seven active CSVs parse with valid widths.
- Exact policy/registry token assertions pass.
- Changed-document links and source-map paths resolve.
- `git -c core.whitespace=cr-at-eol diff --cached --check` passes after final staging.
- Secret scan reports zero findings.
- No economic run, payload reader, protected access, or controls were invoked.

The approval applies only to the documentation/registry policy application. It is not economic validation, a KDA02 pass, control authorization, KDA03 authorization, or deployment approval.
