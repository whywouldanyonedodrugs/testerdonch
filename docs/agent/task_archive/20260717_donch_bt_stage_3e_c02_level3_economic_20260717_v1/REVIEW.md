# Review

Pre-run decision: `approve`.

The reviewed runner enforced the exact four-definition register, safe Kraken PF manifest reads, strict next-open entry, actual timeout exits, definition-local actual-exit non-overlap, lifecycle/protected failure, fixed costs, separate funding, and no Level-4 control surface.

Post-run decision: `approve_with_reporting_amendment`.

The immutable trade, eligibility, and funding-boundary ledgers reconcile and contain no protected rows. Two derived-report defects were corrected without rerunning economics: exact rows carrying the shared label `fully_exact` are now reported under contract label `fully_exact_funded`; and non-concentration gates remain independently reported when a non-positive concentration denominator forces concentration gates to fail closed. The decision did not change.

Neither primary definition passes all frozen Level-3 gates. The 6h primary has positive mean base net but negative median, bootstrap lower bound below -5 bps, excessive concentration, and stress mean below -10 bps. Level-4 controls remain unauthorized.
